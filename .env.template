# ============================================================================
# .env.template — UniEasy Frontend (Vite)
# Copy this to .env.local and fill in your values.
# NEVER commit .env.local — it is in .gitignore.
# All frontend env vars MUST start with VITE_ to be exposed to the client.
# ============================================================================

# Supabase (public anon key only — NEVER use service_role on frontend)
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...your-anon-key...

# Clerk
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...your-clerk-publishable-key...

# API
VITE_API_BASE_URL=http://localhost:8080

# Google Maps center (Christ University Central Campus)
VITE_GOOGLE_MAP_CENTER_LAT=12.934533
VITE_GOOGLE_MAP_CENTER_LNG=77.606861
